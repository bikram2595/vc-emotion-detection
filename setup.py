from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='ml project to determin emotion with version control framework',
    author='bikram',
    license='',
)
